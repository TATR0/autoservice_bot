# Только для разработки. В продакшен-образ не попадает — там requirements.txt.
# Установка:  pip install -r requirements-dev.txt
pytest==9.1.1
pytest-asyncio==1.4.0

# Клиент для тестов HTTP-слоя (starlette.testclient). httpx2 — тот, который
# TestClient использует по умолчанию; httpx нужен ему как зависимость.
httpx==0.28.1
httpx2==2.10.0
